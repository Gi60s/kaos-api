/locations:
  description: Get a list of all locations.
  responses:
    200:
      description: All locations.
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
